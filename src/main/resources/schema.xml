<?xml version="1.0"?>
<!DOCTYPE mycat:schema SYSTEM "schema.dtd">
<mycat:schema xmlns:mycat="http://org.opencloudb/">

	<schema name="test" checkSQLschema="false" sqlMaxLimit="100" dataNode="dn1">
		<!-- auto sharding by id (long) -->
		<table name="travelrecord" dataNode="dn1,dn2,dn3" rule="auto-sharding-long" />

		<!-- global table is auto cloned to all defined data nodes ,so can join 
			with any table whose sharding node is in the same data node -->
		<table name="company" primaryKey="ID" type="global" dataNode="dn1,dn2,dn3" />
		<table name="goods" primaryKey="ID" type="global" dataNode="dn1,dn2" />
		<!-- random sharding using mod sharind rule -->
		<table name="hotnews" primaryKey="ID" dataNode="dn1,dn2,dn3" rule="mod-long" />
		<!--
		<table name="dual" primaryKey="ID" dataNode="dnx,dnoracle2" type="global" needAddLimit="false"/>
	    <table name="worker" primaryKey="ID" dataNode="jdbc_dn1,jdbc_dn2,jdbc_dn3" rule="mod-long" />
		-->
		<table name="employee" primaryKey="ID" dataNode="dn1,dn2"
			rule="sharding-by-intfile" />
		<table name="customer" primaryKey="ID" dataNode="dn1,dn2"
			rule="sharding-by-intfile">
			<childTable name="orders" primaryKey="ID" joinKey="customer_id"
				parentKey="id">
				<childTable name="order_items" joinKey="order_id"
					parentKey="id" />
			</childTable>
			<childTable name="customer_addr" primaryKey="ID" joinKey="customer_id"
				parentKey="id" />
		</table>
		<!--
		<table name="oc_call" primaryKey="ID" dataNode="dn1$0-743" rule="latest-month-calldate" />
		-->
	</schema>

	<!--
	<dataNode name="dn1$0-743" dataHost="localhost1" database="db$0-743" />
	 -->
	<dataNode name="dn1" dataHost="localhost1" database="db1" />
	<dataNode name="dn2" dataHost="localhost2" database="db2" />
	<dataNode name="dn3" dataHost="localhost3" database="db3" />

	<dataHost name="localhost1" maxCon="50" minCon="0" balance="0" writeType="0" dbType="h2" dbDriver="jdbc"
			  switchType="1"  slaveThreshold="100">
		<heartbeat>select 1</heartbeat>
		<!-- can have multi write hosts -->
		<writeHost host="hostM1" url="jdbc:h2:mem:db1" user="sa" password="">
			<!-- can have multi read hosts -->
		</writeHost>
	</dataHost>

    <dataHost name="localhost2" maxCon="50" minCon="0" balance="0" writeType="0" dbType="h2" dbDriver="jdbc"
              switchType="1"  slaveThreshold="100">
        <heartbeat></heartbeat>
        <!-- can have multi write hosts -->
        <writeHost host="hostM2" url="jdbc:h2:mem:db2" user="sa" password="">
            <!-- can have multi read hosts -->
        </writeHost>
    </dataHost>

    <dataHost name="localhost3" maxCon="50" minCon="0" balance="0" writeType="0" dbType="h2" dbDriver="jdbc"
              switchType="1"  slaveThreshold="100">
        <heartbeat></heartbeat>
        <!-- can have multi write hosts -->
        <writeHost host="hostM3" url="jdbc:h2:mem:db3" user="sa" password="">
            <!-- can have multi read hosts -->
        </writeHost>
    </dataHost>

</mycat:schema>
